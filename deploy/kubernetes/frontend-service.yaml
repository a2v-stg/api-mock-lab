apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: mocklab
  labels:
    app: mocklab
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: mocklab
    component: frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
